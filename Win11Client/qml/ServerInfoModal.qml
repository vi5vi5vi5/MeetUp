import QtQuick
import MeetUp

// «Сервер о себе» на экране входа: адрес человек выбирает там, и вопросы
// про сервер возникают там же — «это тот сервер?», «он вообще жив?»,
// «почему тут нет регистрации?». Отвечать на них после входа поздно.
//
// Содержимое общее с разделом «О программе» — см. ServerInfoPanel.
AppModal {
    id: root

    title: "Сервер"
    subtitle: "Что этот сервер рассказывает о себе"
    modalWidth: 480

    // Открыли — перезамерили: старый отклик хуже, чем никакой.
    onOpenChanged: if (open) Server.refresh()

    ServerInfoPanel { width: parent.width }
}
